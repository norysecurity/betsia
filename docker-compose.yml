services:
  betting_ai:
    build: .
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - API_FOOTBALL_KEY=${API_FOOTBALL_KEY}
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
      - LEAGUE_ID=${LEAGUE_ID}
      - SEASON=${SEASON}
